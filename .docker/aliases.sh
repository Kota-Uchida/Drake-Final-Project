# Custom Aliases
# This aliases are automatically read when the docker is built.

alias cddrake='cd /home/drake'
alias build='/home/drake/build.sh'
alias reloadaliases='source /etc/profile.d/aliases.sh'

